models:
  - type: main
    engine: openai
    model: llama3
    parameters:
      base_url: "http://localhost:11434/v1"
      api_key: "ollama"

rails:
  input:
    flows:
      - check medical safety